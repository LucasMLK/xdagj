pool {
  id = "test-pool-node2"
  name = "Test Pool for Node2"

  node {
    rpcUrl = "http://localhost:10002"  # 连接Node2
    timeout = 30s
    maxRetries = 3
    retryDelay = 5s
  }

  stratum {
    host = "0.0.0.0"
    port = 3334  # Pool2使用不同端口
    initialDifficulty = 1000000000
  }

  mining {
    blockInterval = 64s
  }

  database {
    type = "memory"
  }
}
