miner {
  # Pool connection
  pool {
    host = "localhost"
    port = 3333
    reconnectDelay = 10s
    timeout = 30s
  }

  # Worker identity
  worker {
    address = "nCR7vmdnVzsZ24VtqqurSmC7WLMF8Ymj"  # Valid Base58Check test address
    name = "test-miner-1"
    password = "x"
  }

  # Mining settings
  mining {
    threads = 2  # Use 2 threads for testing
    algorithm = "auto"  # auto-detect (sha256 or randomx)
    nonceRange = 10000
  }

  # Statistics
  stats {
    interval = 60s
    verbose = false
  }
}
