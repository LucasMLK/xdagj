pool {
  id = "test-pool-node1"
  name = "Test Pool for Node1"

  node {
    rpcUrl = "http://localhost:10001"
    timeout = 30s
    maxRetries = 3
    retryDelay = 5s
  }

  stratum {
    host = "0.0.0.0"
    port = 3333
    initialDifficulty = 1000000000  # 提高到10亿，减少share频率
  }

  mining {
    blockInterval = 64s
  }

  database {
    type = "memory"
  }
}
